from selenium import webdriver
from selenium.webdriver.chrome.service import Service
from selenium.webdriver.common.by import By
from selenium.webdriver.common.keys import  Keys

service = Service("D:\Downloads\chromedriver_win32\chromedriver")
driver = webdriver.Chrome(service = service)

driver.get("https://www.python.org/")




dates=driver.find_elements(By.CSS_SELECTOR,'.event-widget time')
event_names = driver.find_elements(By.CSS_SELECTOR , '.event-widget li a')
events  = {}


for n in range(0,len(event_names)):
    events[n] = {
        "time":dates[n].text,
        "event":event_names[n].text
    }

print(events)
# for event in time:
#
#     print(f"2022-{event.text}")
#
# for link in links:
#     print(link.text)

# for event in events:
#     dictionary = {
#     "dates" : event.text,
#     "event": event.get_attribute('href')
#     }

# print(dictionary)




driver.close()
# requested_number = driver.find_element(By.CSS_SELECTOR,"#articlecount").click()

# all_portals = driver.find_element(By.LINK_TEXT , "All portals")
# print(all_portals.text)
# search = driver.find_element(By.NAME,"search")
# search.send_keys("Python")
# search.send_keys(Keys.ENTER)

# first_name = driver.find_element(By.NAME , "fName")
# last_name = driver.find_element(By.NAME, "lName")
# email = driver.find_element(By.NAME, "email")
# sign_up = driver.find_element(By.CSS_SELECTOR,"form button")
# first_name.send_keys("Nerya")
# last_name.send_keys("Manassen")
# email.send_keys("neryamanassen9@gmail.com")
# sign_up.send_keys(Keys.ENTER)

